# R1 実装・検査記録

実施日：2026-09-22（日本時間）
対象：PR #5「完成版 実装計画書 v2.0」のR1
基準：main `097ef3db51444fa7eac6c82ddab0b8e97fde1943`
作業ブランチ：`work/paripari-r1-integration-20260922`

## 統合元と変更

| 対象 | 統合元と今回の扱い |
| --- | --- |
| ゲームのルール・描画 | A `be1719efabd1d3e28d8e1ef44969a4d88b46a659` の5方向・20段階・最大3分割を保持。config/game/enemy/judge/scoring/renderer/particlesのルールを変更しない |
| ホーム・名前・結果・実験場リンク | B `bcc66037bfd7ac5bd33cc7983531169034ed1972` のindex/main/styleから該当部品のみ移植。Bの2→4方向本体で上書きしない |
| 共有 | Bのplatform内の共有→コピー→欄選択という処理を分離して再実装。キャンセル時は何もしない。ホームと結果は各画面内の欄を選ぶ |
| 通信 | Bのランキング呼び出し、接続先・キー、ランキング欄・通信失敗表示は移植しない |
| 記録 | 旧得点を残し、新キー `paripari.best.r1-5dir-20260922` へ分離。旧名のキーは引き継ぐ。読み書きの例外を捕捉し、不正な型を採用しない |
| 入力・表示 | 名前欄・日本語変換中・メニューでゲーム用キーを奪わない。横長PCの回転案内を修正。名前・共有欄を加えたメニューへ到達できるようにする |
| 配布 | 手作業のモジュール一覧を廃止。esbuild 0.28.2でimportを追跡して生成し、不足ファイル・存在しないexportを生成時に失敗させる。実行時の外部依存は追加しない |
| 初期の自動検査 | 既存31件を保持。生成・名前・保存・共有の10件と、分割版/単一HTMLのブラウザ検査を追加。Qualityは検査のみで公開しない |

任意練習の完了後は「遊び方」へ戻します。保存済みの古い名前で本番へ自動的に進んでしまう経路を、R1の名前統合の修正として先行して閉じました。練習で成功した方向だけ進める仕組みはR3です。

正式公開URLは `OFFICIAL_GAME_URL = ''` とし、開発URL・ローカルファイルのURLを共有しません。R6で正式URLを確認します。共有に必要な名前は本番開始時に確定し、再挑戦では再入力させません。

名前は制御文字と前後空白を取り除き、最大20文字とします。対応環境では `Intl.Segmenter` により合成絵文字を1文字として扱います。非対応環境のフォールバックはコードポイント単位です。旧得点と試作R1の記録は、R2/R4でルールを変える際にも混在させないでください。

## 実施した検査

| 検査 | 現在の結果 | 根拠・範囲 |
| --- | --- | --- |
| `npm test` | 41件合格 | 既存31件＋新規10件。生成4件、名前・保存・共有6件 |
| `npm run build` | 合格 | 分割ソースから単一HTMLを生成。13モジュールを同梱 |
| `npm run build:check` | 合格 | 再生成の内容とdistが一致 |
| `git diff --check` | 合格 | 空白エラーなし |
| ローカルブラウザ検査 | Chromium 13ケース合格 | 分割/生成HTML、PC/タッチ、成功/失敗/結果/再挑戦、共有・保存非対応、任意練習・IME・回転の回帰。WebKitはこの環境では未実施 |
| GitHub Actions | 合格 | [Quality #2](https://github.com/chameleonjp-lab/paripari/actions/runs/35633180292)：単体41件、配布物一致、Chromium 13件＋WebKit 13件が全て合格。片方を自動スキップしない |
| 独立レビュー | 完了・R1未解決指摘0 | Sol Highが別コンテキストで差分・実行結果を確認。修正後も41件とChromium13ケースを独立再検査 |
| iPhone実機 | 未実施 | 模擬寸法とLinux WebKitを実機確認と扱わない |

B01は必要ファイルを削除した一時コピーと、存在しない関数のimportを追加した一時コピーが失敗することを確認しています。B05は手作業の同梱リストにない新しいimportを含むことと、2回の生成結果が一致することを確認しています。

D01/D02/D03/D04、H01/H02/H03の一部を単体検査しています。名前の20文字境界、合成絵文字、空白、旧得点保全、不正保存値、保存例外、低得点の後保存、共有キャンセル、URL未設定、コピー・選択への代替を含みます。受入ID全体の合格を意味せず、実ブラウザや複数タブ等の未実施条件は後続検査で補います。

ローカルブラウザ検査コマンド（Chromiumだけを明示した部分実施）:

```sh
PARIPARI_BROWSERS=chromium \
PARIPARI_CHROMIUM_EXECUTABLE_PATH=/path/to/chromium \
PARIPARI_ARTIFACT_DIR=/tmp/paripari-r1-final npm run test:browser
```

Qualityではこれらの部分実施指定を付けず `npm run test:browser` を実行します。

## 実施環境と証拠

- Node.js 24.19.0、Playwright 1.62.1、esbuild 0.28.2。
- ローカルChromiumは153.0.8010.0を使用。通常のPlaywrightブラウザ取得がタイムアウトしたため、作業環境のみ別配布のChromiumを指定。プロジェクトの依存には追加していない。
- ローカルWebKitの取得は成功したが、必要なOSライブラリがなく起動不可。OS側の依存導入も環境の権限エラーで実行不可。GitHub Actionsで標準のChromium/WebKitを検査する。
- ローカル検査ではスクリーンショット22枚を作成。Chromiumのみの実行であることをテスト出力でも明示。
- GitHub Actionsの検査対象は `8edddfe554ce9b1ebf7873dee116bf9eb8d22578`。Ubuntu 24.04、Chromium 151.0.7922.34、WebKit 26.5（Playwright revision 2336）で26件が合格。[スクリーンショット44枚](https://github.com/chameleonjp-lab/paripari/actions/runs/35633180292/artifacts/10655133083)を保存。
- 証拠はブラウザ検査の出力とQualityの `r1-browser-evidence` に残す。通常の端末記録とは隔離する。

## 修正とレビュー

実装：Luna Max。ブラウザ検査作成・実行：別のLuna Max。独立レビュー：Sol High。統合と生成・単体検査・進捗記録・PR作成は親担当。

実装中の検査で、合成絵文字が名前の末尾で切れることと、保存値のscoreが文字列でも採用されることを検出し、修正後に合格を確認しました。

独立レビューでは、保存済みの名前があるとホーム欄の編集を無視して任意練習から本番へ進める不整合を指摘されました。任意練習後は遊び方へ戻すように修正しました。親の確認で、PC中央540pxのゲーム領域、結果の主要2ボタンを共有欄より前へ置くこと、ジェスチャー抑止をゲーム領域へ限定することも修正しています。

独立レビューではさらに、1024×768のタッチ併用PCが回転案内で覆われる問題を検出しました。画面幅とタッチ機能だけの判定をやめ、モバイル端末の情報を確認し、向きはscreen.orientationを優先するよう修正しました。回帰にタッチ併用PCとiPhone相当の横向きを加えています。

検査側のconsole.error未判定も修正しました。Sol Highによる最終再レビューで、R1範囲の未解決High/Medium/Low指摘は0件となりました。単体41件、配布物一致、Chromium13ケースを別担当が再実行し、全て合格しました。WebKitとiPhone実機の確認をこの判定へ含めていません。

初回の[Quality実行](https://github.com/chameleonjp-lab/paripari/actions/runs/35632165345)では、単体41件・配布物一致が合格し、ブラウザ検査は25件合格・WebKit横向き案内の1件が失敗しました。検査には固定100ミリ秒待機があり、製品の回転後200ミリ秒待機より短いため、案内の表示状態を期限付きで待つ方式へ変更しました。失敗時には画面・向きの診断を残し、縦向きへ戻した後の案内消滅も必須検査に加えました。製品の向き判定はこの検査修正では変更していません。Sol Highがこの差分を再レビューして追加指摘なしと確認し、Quality #2で26件全てが合格しました。

## 残作業・公開の扱い

R2〜R6は未完了です。既存の時計・停止復帰、練習集計、練習の成功条件、難易度の登場時期、日本語説明、全画面・文字拡大・読み上げ、長時間負荷、1,000系列の分布、正式URL・プレビュー画像・実機確認を今回の合格範囲へ含めません。

ゲームを公開したり、実験場の一覧へ追加したり、ランキング連携したりしていません。mainのマージはユーザーが行います。
