{
  "name": "paripari",
  "version": "1.0.0",
  "description": "敵の攻撃に合わせて反対方向を弾く、ジャストタイミング受け流しゲーム",
  "type": "module",
  "scripts": {
    "test": "node tests/test.mjs && node --test tests/*.test.mjs",
    "build": "node build.mjs",
    "test:browser": "node tests/browser.mjs",
    "build:check": "node build.mjs --check"
  },
  "license": "MIT",
  "devDependencies": {
    "esbuild": "0.28.2",
    "playwright": "1.62.1"
  }
}
